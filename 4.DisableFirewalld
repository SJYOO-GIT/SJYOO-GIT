---
- hosts: "{{ RUNSVR }}"
  tasks:
    - name: Disable firewalld
      service:
        name: firewalld
        state: stopped
        enabled: false
      when:
        firewalld.state == "started" || firewalld.state == "restarted"